# Start watchdog at boot time? 0 or 1
run_watchdog=1

# Load module before starting watchdog
watchdog_module="<% if @architecture == 'armv6l' %>bcm2708_wdog<%else %>softdog<% end %>"

# Specify additional watchdog options here (see manpage).

